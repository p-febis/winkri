query GetCollection($slug: String!, $channel: String!) {
  collection(slug: $slug, channel: $channel) {
    id
    name
    slug
    description

    products(first: 100) {
      edges {
        node {
          ...ProductListItem
        }
      }
    }
  }
}
